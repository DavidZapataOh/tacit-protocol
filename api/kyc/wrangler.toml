name = "tacit-kyc-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
ENVIRONMENT = "production"

# KV namespace for dynamically seeded wallets (Plan 2.2 seed endpoint)
# Uncomment after creating KV namespace with: wrangler kv namespace create "KYC_KV"
# [[kv_namespaces]]
# binding = "KYC_KV"
# id = "<KV_NAMESPACE_ID>"
# preview_id = "<PREVIEW_KV_NAMESPACE_ID>"
