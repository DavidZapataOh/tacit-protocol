# Secret name mappings for Tacit settlement workflow
# These map workflow secret references to Vault DON secret identifiers.
# Actual values are uploaded via `cre secrets upload` (never stored here).
secretsNames:
  OFAC_API_KEY:
    - TACIT_OFAC_API_KEY
  KYC_API_KEY:
    - TACIT_KYC_API_KEY
